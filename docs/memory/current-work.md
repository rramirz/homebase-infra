# Current Work

## Active Goal

Improve OpenCode context efficiency and model routing.

## Current Status

- 2026-09-01: upgraded live Helm release `flannel` (CNI) in namespace `kube-flannel` from official chart `v0.24.2` to `v0.28.9`. Root cause of the investigation: Plane CE pilot instability traced to `home-base2`/`home-base4` briefly going `NotReady` (unrelated Proxmox activity), not to Flannel version; upgrade was a planned hardening, not an incident fix. Replaced the repo's stale, untracked-as-Helm raw `v0.12.0` manifest (`charts/flannel/deploy.yaml`, actually a leftover from before the live cluster was migrated to the official Helm chart in Feb 2024) with a proper wrapper chart at `charts/flannel` (parent `apiVersion: v2`, dependency `flannel` `v0.28.9` from `https://flannel-io.github.io/flannel`, vendored `charts/flannel/charts/flannel.tgz` gitignored via existing `charts/**/charts/` rule, values.yaml pins only `flannel.podCidr: "10.244.0.0/16"`). Added guarded `task flannel:{dependency,lint,render,diff,dry-run,upgrade,rollback,verify}` to `Taskfile.yaml`, all context-gated to `home-base` and dependency-rebuilding from `Chart.lock` since the vendored `.tgz` is gitignored. Consulted Oracle before the live change; verdict was conditional GO requiring a 30-minute stable baseline, pre-pulled images both directions, and `--reset-values --wait --timeout 10m` (no `--atomic`/`--force`/`--recreate-pods`/`--reuse-values`). Preflight: read `/proc/sys/net/bridge/bridge-nf-call-iptables` and `/proc/sys/net/ipv4/ip_forward` directly over plain SSH (not Ansible `-b`, which needs an interactive sudo password on this inventory, and not `sysctl`, which isn't in the non-root PATH on these nodes) — confirmed `br_netfilter`/forwarding/valid distinct `/run/flannel/subnet.env` on all 5 nodes. Pre-pulled the two new `ghcr.io/flannel-io/*` images on every node via a temporary throwaway DaemonSet (`kubectl apply`/`delete`, no `crictl`/sudo needed) rather than Ansible. Ran baseline DNS + full 25-path all-to-all ping from one `busybox` pod per node in a scratch `flannel-netcheck` namespace before and after the upgrade (post-upgrade pods recreated fresh to exercise the new CNI binary's ADD path). Live upgrade: `helm upgrade flannel charts/flannel -n kube-flannel --reset-values --wait --timeout 10m` → revision 2, `deployed`, DS rolled 5/5 with `maxUnavailable: 1`. Verified: live DS images exactly `ghcr.io/flannel-io/flannel:v0.28.9` + `flannel-cni-plugin:v1.9.1-flannel3`, ConfigMap still `10.244.0.0/16`/vxlan, health probes/`NoExecute` toleration present, flannel logs show clean VXLAN startup on all 5 nodes. 15-minute post-upgrade observation window: 5/5 nodes Ready, DS 5/5 available, zero flannel pod restarts throughout. Cleaned up the netcheck namespace and prepull DaemonSet. Rollback path (untested live, but Helm revision 1 confirmed intact/`superseded`): `task flannel:rollback` → `helm rollback flannel 1 -n kube-flannel --wait --timeout 10m`. See `AGENTS.md` "Known Version Debt" and `docs/deployment.md` "Flannel CNI upgrade" for the full runbook.
- 2026-08-26: hardened the guarded PVE 8/bookworm → PVE 9/trixie transition after pve-1's partially completed package migration. Production policy now uses correct single-quoted PVE 6.14/6.17/7+ kernel regexes, resets repeated source-policy accumulators, creates a root-only non-overwriting backup before disabling regenerated `pve-enterprise.sources`, revalidates exact canonical Debian plus `pve-no-subscription` sources after target/resume flow, and blocks reboot/verify on an active enterprise, unknown, or noncanonical source. Target/reboot/verify also require a concrete signed kernel package and matching `/boot/vmlinuz-*`; `proxmox-kernel-7.0` meta alone cannot pass. Local-only checks passed (`ansible-playbook --syntax-check`, package fixture, 14 policy tests); no hosts, packages, or reboots were touched. Next operational step remains guarded `resume` only after the generated enterprise source is disabled by this code.
- 2026-08-24: added local-only Proxmox maintenance automation; no live hosts were contacted or changed. `task update:proxmox-packages-no-reboot` runs serial `[proxmox]` package maintenance and reports reboot requirements; `...with-reboot` is separately prompt-gated. Guarded one-host PVE 8/bookworm → PVE 9/trixie phases live under `task proxmox:major:{preflight,upgrade,reboot,verify}` and require exact `PVE_HOST` plus explicit acknowledgements. See `docs/runbooks/proxmox-major-upgrade.md`. This records automation only, not an executed upgrade.
- OpenCode global routing moved toward cheap orchestrator + specialized agents.
- Fable policy changed to conscious opt-in, not default.
- Repo memory scaffold added for on-demand context docs.
- 2026-06-13: added and applied `dipradar.io` support for Gateway/cert-manager/external-dns. Same Cloudflare API token is reused; no new secret required. Applied `ClusterIssuer` `letsencrypt-cloudflare`; upgraded Helm releases `kgateway` to revision 7 and `external-dns` to revision 14. Gateway `home` now has HTTPS listeners for `dipradar.io` and `*.dipradar.io` with cert refs `dipradar-io-tls` and `dipradar-io-wildcard-tls`. Both certs are Ready/valid. ExternalDNS domain filters now include `theramirez.casa`, `homeapps.io`, and `dipradar.io`. Cert-manager ClusterIssuer solver is scoped to those DNS zones.
- 2026-06-29: upgraded Helm release `my-n8n` in namespace `8n8` to latest stable n8n version `2.27.5`. Local chart [Chart.yaml](file:///Users/rafaelramirez/homespace/homebase-infra/charts/n8n/Chart.yaml) appVersion updated to `2.27.5` and chart version bumped to `1.2.0`. Image tag in [values.yaml](file:///Users/rafaelramirez/homespace/homebase-infra/charts/n8n/values.yaml) was updated to `2.27.5`. Rollout was successful, and SQLite database migration completed cleanly on startup.
- 2026-07-05: confirmed Home Assistant latest upstream release is `2026.7.1` and applied local chart [charts/home-assistant](file:///Users/rafaelramirez/homespace/homebase-infra/charts/home-assistant) as Helm release `home-assit` revision 20 in namespace `home-assistant`. Chart version is `0.2.2`; appVersion and image tag are `2026.7.1`. Rollout succeeded and `https://homeassistant.theramirez.casa` returned HTTP 200.
- 2026-07-05: installed Mealie recipe manager with local chart [charts/mealie](file:///Users/rafaelramirez/homespace/homebase-infra/charts/mealie) as Helm release `mealie` revision 1 in namespace `mealie`. Image is `ghcr.io/mealie-recipes/mealie:v3.20.1`; storage is SQLite on PVC `mealie-data` (`ssd`, 10Gi, mounted at `/app/data`); signup disabled; hostname is `https://mealie.theramirez.casa` through kgateway HTTPRoute `mealie`. Rollout succeeded, route Accepted/ResolvedRefs=True, and `/api/app/about` plus `/` returned HTTP 200. First login uses upstream default `changeme@example.com` / `MyPassword`; change password after login.
- 2026-09-01: Plane CE `v1.3.1` pilot is live at Helm release/namespace `plane`, revision 5. Right-sizing succeeded after replacing API/worker/beat/live resources: current memory is API `362Mi`, worker `496Mi`, beat `169Mi`, live `167Mi`; Prometheus 7-day peaks are approximately `365/500/172/169Mi`. New sizing is API `384Mi` request/`512Mi` limit, worker `512Mi`/`768Mi`, beat/live `192Mi`/`256Mi`; total steady-state Deployment requests are `1500m` CPU/`2816Mi` memory, below the 3 CPU/10 GiB ceiling. All 12 Deployments were Available, `plane-migrator-5` completed (migrations no-op), both HTTPRoutes were Accepted/Resolved, and app HTTP returned 200. Pods were verified with zero restarts over a 5-minute observation. The fixed-name `plane-seaweedfs-bucket` Job failed at `activeDeadlineSeconds=300` because `weed shell` needs unexposed gRPC ports `19333`/`18888`; it was deleted after the API confirmed `Bucket plane-uploads exists`. This is unrelated to the rollout. Prior restarts were node/Valkey outage, not OOM; resource sizing does not fix the earlier Redis `ECONNREFUSED` outage. Pilot remains disposable and LAN/VPN-only; no real data until off-cluster backup/restore exists. Presigned upload and backup restore remain unverified.

- 2026-08-02: repaired qBittorrent WebUI authentication after its persisted password disappeared during a restart; synced the new password into Sonarr and Radarr and verified both client tests survive a qBittorrent restart. Password is not stored in repo/memory. Set Prowlarr TPB Apps Minimum Seeders to 1 (Prowlarr Full Sync is source of truth). Imported The Challenge Season 33 E01-E16 and Season 34 E01-E18 from two public Drive folders into the NAS TV path, normalized filenames/permissions, rescanned Sonarr, and removed nine redundant torrents. All 34 files passed `ffprobe` as H.264 1280x720; reunion mapping was verified. Final Sonarr state: S33 `16/18` (reunions missing), S34 `18/18`; queue empty; only health warning is flaky TorrentGalaxyClone.
- 2026-08-12: completed `pve-2` / `infra` freeze remediation. Updated Proxmox `8.4.14` -> `8.4.20`, kernel `6.8.12-16-pve` -> `6.8.12-41-pve`, added `non-free-firmware`, and installed `amd64-microcode` `3.20250311.1~deb12u1`, updating early microcode `0x08608103` -> `0x08608108`. Removed GRUB `tsc=unstable` and invalid `amd_iommu=on`; kernel detects the 3 ms TSC skew/broken BIOS and falls back to HPET itself. Controlled recovery restored all four pve-2 VMs and all five Kubernetes nodes to Ready, with API/etcd ready and only pre-existing `qualtrim-sync` failures remaining. Both SSD SMART checks passed; `SSD` VG remains 98.55% provisioned. Rollback and validated off-host etcd snapshot details are in `AGENTS.md`. Next if freezes recur: BIOS `SER5H508` update after backup/release-note review, then power-brick, Memtest86+, and thermal testing. BIOS was not updated.
- 2026-08-13: built initial `apps/family-retirement-planner` milestone: Go/Gin/PostgreSQL monolith, native auth/CSRF, user+household scoped repositories, deterministic decimal retirement engine, CRUD, readiness/timeline/year ledger, scenario overrides/comparison, Dockerfile, Compose, adjacent Helm chart, and architecture docs. Verified with Go unit/race/static tests, real PostgreSQL migration/isolation tests, `govulncheck` (zero reachable vulnerabilities), Helm lint/render, linux/amd64 Docker build, and Playwright browser QA covering registration/login, Mom & Dad data entry, projection/year detail, scenario comparison, CSS delivery, and separate-household data isolation. Not deployed. LAN/VPN-only; no real financial data until encrypted off-cluster backup/restore exists and is tested.
- 2026-08-14: replaced failed placeholder estate work in `apps/family-retirement-planner` with a complete authenticated household estate MVP. Added scoped PostgreSQL tables/composite FKs for beneficiaries, trusts, allocations, assets, owners, designations, insurance, documents, contacts, digital metadata, residuary shares, scenarios, and immutable input/result snapshots; typed repository CRUD; dedicated forms; strong disclaimer/credential prohibition; and pure decimal eight-stage engine (insurance, WROS, designation, trust, probate, obligations, residuary, liquidity/solvency warnings). Real PostgreSQL tests use two registered users/households and actual IDs, reject cross-scope routes/repository calls and a forged composite FK, and persist/reload a deterministic result. Not deployed; same backup and LAN/VPN-only restrictions remain.
- 2026-08-14: estate audit follow-up added primary/contingent tiers to trust/asset/insurance designations (each recorded tier independently totals 100%; primary distributes, contingent is readiness metadata), dependent beneficiaries, scenario as-of/death years, funeral/legal costs, optional debt payment, explicit obligation output, and pure typed readiness for household document inventory, executor/guardian, POA/directive, designation tiers, residuary, and liquidity. Readiness is shown on the dashboard and persisted per result; document checks explicitly remain household-level, not per-adult legal validation. Pre-deploy migration `003_estate.sql` was rewritten and mirrored byte-identically; fresh Docker PostgreSQL integration passed.
- 2026-08-14: final estate audit fixes made global stage validation independent of UUIDs, require WROS decedent ownership plus a distinct survivor, snapshot immutable scenario display metadata in results through migration `004`, preserve user+household-scoped results after scenario deletion through composite `(scenario_id,user_id,household_id)` FK behavior, and reject credential-like tokens in every digital metadata field before create/update. Not deployed; LAN/VPN-only and no-real-data-until-encrypted-off-cluster-backup-plus-tested-restore restrictions remain. `gofmt`, `go vet`, unit, race, fresh PostgreSQL integration, `govulncheck`, Helm, and browser smoke tests passed; final correctness and security Oracle reviews passed. Docker image build after fixes was not reconfirmed because local BuildKit canceled after tests, though the pre-fix build passed.

## Next Steps

- Keep `AGENTS.md` stable; move future volatile notes into memory/topic docs.
- Use `scripts/update-agent-memory.sh` after meaningful repo context changes.
- Plane CE pilot: observe memory and restart behavior for 24 hours and 7 days; fix the SeaweedFS bucket Job before the next Helm upgrade; then test a real presigned upload and build/test backup restore.

## Open Questions

- Whether to later compact the existing large `AGENTS.md` into topic docs.

## Last Updated

2026-09-01
