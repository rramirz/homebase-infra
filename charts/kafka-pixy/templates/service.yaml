apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-pixy.fullname" . }}
  labels:
    {{- include "kafka-pixy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-pixy.selectorLabels" . | nindent 4 }}
