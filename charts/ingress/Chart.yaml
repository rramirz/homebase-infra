apiVersion: v2
name: homeapps
description: A Helm chart to expose local-network services through kgateway (Gateway API) using Backend (Static) + HTTPRoute, with optional upstream TLS via BackendConfigPolicy.
type: application
version: 0.2.0
appVersion: "1.0"

