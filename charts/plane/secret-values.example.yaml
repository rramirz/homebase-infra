# Example only. Copy to secret-values.yaml (gitignored) and fill with real
# generated credentials. Never commit secret-values.yaml or paste real values
# into this example file.
secrets:
  postgres:
    user: plane
    password: CHANGE_ME
  rabbitmq:
    user: plane
    password: CHANGE_ME
  django:
    secretKey: CHANGE_ME
  live:
    secretKey: CHANGE_ME
  seaweedfs:
    s3AccessKeyId: CHANGE_ME
    s3SecretAccessKey: CHANGE_ME
